name: Staging Build
on:
  push:
    branches:
      - release/**

jobs:
  staging-build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18.x
          cache: npm

      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          expo-cache: true
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        run:
          yarn

          # Set NODE_ENV to staging
      - name: Set NODE_ENV to Staging
        run: echo "NODE_ENV=test" >> $GITHUB_ENV

      - name: Configure EAS Update
        run: eas update:configure

      - name: Publish to EAS Update
        if: env.NODE_ENV == 'test'
        run: |
          BRANCH_NAME=preview-${{ github.run_id }}
          eas update --branch $BRANCH_NAME --non-interactive --message "Preview #${{ github.run_id }}"

      - name: Add EAS Update Link to Staging
        if: env.NODE_ENV == 'test'
        uses: mshick/add-pr-comment@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          message: |
            ## Test this Staging with EAS Update
            ![Expo QR](https://api.qrserver.com/v1/create-qr-code/?size=250x250&data=exp://u.expo.dev/${{ secrets.EXPO_ACCOUNT_NAME }}/vifin-app?channel-name=preview-${{ github.run_id }})
            **------ Build published to [EAS Update](https://expo.dev/accounts/${{ secrets.EXPO_ACCOUNT_NAME }}/projects/vifin-app/branches/preview-${{ github.run_id }}) ------**
